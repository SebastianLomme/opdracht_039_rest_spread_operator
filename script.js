const addAll = (...num) => num.reduce((prev, current) => prev + current);


console.log(addAll(1,2,3,4));

const Optellen = (num1, num2, num3) => sum = num1 + num2 + num3;


const arrayNumber = [1,2,3];

console.log(Optellen(...arrayNumber));